#include <iostream>
using namespace std;

int sumCoins(int n) {
    if (n == 0)
      return 0;        
    return n + sumCoins(n - 1); 
}

int main() {
    int n;
    cin >> n;
    cout << sumCoins(n);
    return 0;
}
